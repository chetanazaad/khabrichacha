# UI & Framework
nicegui>=2.0.0
uvicorn>=0.28.0
watchdog>=4.0.0

# Logging & Terminal
loguru>=0.7.2
rich>=13.7.0

# LLM Providers & AI
openai>=1.12.0
google-generativeai>=0.4.0

# Web Scraping, Browsing & PDF/Word Tools
# NOTE: "duckduckgo-search" was frozen by its maintainer in mid-2025 and
# renamed to "ddgs" (same DDGS class/API) -- installing the old name means
# no further fixes for DuckDuckGo's anti-bot changes. Use ddgs instead.
ddgs>=9.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.1
lxml>=4.9.0
PyMuPDF>=1.23.0
feedparser>=6.0.10
# Headless-browser fallback for JavaScript-rendered pages (fetch_page.py).
# After installing, also run: playwright install chromium
playwright>=1.42.0

# Data & Execution
pandas>=2.0.0
numpy>=1.24.0

# Core Utilities
pydantic>=2.0.0
PyYAML>=6.0.1

# Development
pytest>=8.0.0

# Report Export (PDF + Word)
reportlab>=4.0.0
python-docx>=1.1.0

# Optional: only needed for the local Hugging Face ("transformers")
# provider. The recommended free/local path is Ollama, which needs no
# extra Python packages here -- just the Ollama app + `ollama pull qwen2.5:3b`.
# Install these two only if you specifically want the Transformers provider:
#   pip install transformers>=4.38.0 torch>=2.2.0
