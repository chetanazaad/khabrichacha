# ============================================================
# KhabriChacha — Google Colab Override
# ============================================================

workspace:
  root: "/content/drive/MyDrive/KhabriChacha"

server:
  host: "0.0.0.0"
  port: 8080

browser:
  playwright: true
  chromium: true

logging:
  level: "INFO"
  file: true
