# ============================================================
# KhabriChacha — Docker Override
# ============================================================

workspace:
  root: "/app/workspace"

server:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "INFO"
  file: true
